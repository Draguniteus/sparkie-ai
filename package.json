{
  "name": "sparkie-root",
  "version": "1.0.0",
  "description": "Root config for Sparkie AI - do not install dependencies here",
  "private": true,
  "scripts": {
    "build": "echo 'Build handled by app.yaml'",
    "start": "echo 'Start handled by app.yaml'"
  }
}
